.question-marked
  %h2= t.questions.question_of_the_day
  - unless question.image.blank?
    = render 'questions/item_with_image', question: question
  - else
    = render 'questions/item', question: question