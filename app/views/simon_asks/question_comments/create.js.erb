var context = ".comment-item#<%= @comment.id %>";
$('#question-comments-list').prepend("<%= j render(partial: 'comment', locals: {comment: @comment, question: @question}) %>");
$('#add_question_comment.loader').hide();
$('#question-comment-textarea').val('');

SimonAsks.prepareCommentsLinks(context);
SimonAsks.prepareHints(context);
