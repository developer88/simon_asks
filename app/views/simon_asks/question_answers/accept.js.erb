<% if @uncheck_items %>
$('.answer-item').each(function(){ $(this).find('a.accept_link').removeClass('accepted'); });
<% end %>
<% if @check_item %>
$('.answer-item').each(function(){ if($(this).attr('id') == <%= @answer.id %>){  $(this).find('a.accept_link').addClass('accepted');  } });
<% end %>
